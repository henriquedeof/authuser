package com.xpto.distancelearning.authuser.enums;

// henrique: It must have the prefix ROLE_ to be recognized by Spring Security
public enum RoleType {
    ROLE_STUDENT,
    ROLE_INSTRUCTOR,
    ROLE_ADMIN,
    ROLE_USER;
}